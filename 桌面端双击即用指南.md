# 🏠 变成【像 Eagle 一样】的独立桌面客户端程序 —— 完美绿色版运行指南!

你好呀！我完全看懂你发的小黑窗报错截图了！
* **报错原因**：Windows系统的黑色命令行窗口（CMD）对中文和特殊符号默认字符集支持极度混乱，再加上您本地可能还没成功走完 Node.js 的安装，双击运行时，CMD会把不兼容的字元解释成错误指令，从而出现乱码、闪退。
* **全新终极方案（免安装、免命令行、零门槛）**：为了真正实现您期待的“一键傻瓜式”体验，**我已经直接在云端服务器为您将 Windows 桌面软件完全包好、编译通过了！** 

您不需要在本地运行任何控制台脚本，也不需要配置任何复杂的 Node/Npm，直接看下面的“终极免安装版方案”，双击就能立刻用起来！

---

## 🔥 🌟 终极免安装方案：云端直接打包成果（强力推荐，零环境门槛）

我们已经为您把整个项目完美的绿色免安装 Windows 程序包编译在了压缩包内。

### 1️⃣ 第一步：下载项目压缩包
* 网页右上角点击 **“齿轮”图标 (Settings)** -> 选择 **"Export to ZIP"（导出为 ZIP）**。

### 2️⃣ 第二步：解压
* 将下载回来的 ZIP 压缩包，在电脑上**鼠标右键解压**出来一个文件夹。

### 3️⃣ 第三步：直接双击，开箱即用！
* 进到你解压出来的文件夹，你会看到一个非常显眼的中文名字文件夹：
  👉 `本地素材管理器_Windows桌面版_双击即用`
* 打开它，在里面找到这个文件并**双击运行它**：
  👉 **`本地素材管理器.exe`**
* **见证奇迹的时刻**：
  * 一个带有自定义原生边框，**完全没有浏览器地址栏、100% 独立且本地运行**、像 Eagle 软件一模一样的精致桌面大窗口就会完美浮现在你面前！
  * 它的数据库、导入的数据完全保存在你本地电脑文件夹中，不依赖任何线上网络！
  * **您可以将这个 `本地素材管理器_Windows桌面版_双击即用` 文件夹发送给任何人或挪到 D 盘任意位置，右键把 `本地素材管理器.exe` 发送到桌面快捷方式即可！**

---

## 🚀 另外两套备用方案（如果您想在本地自主重新打包）

如果您想要自己打包，以下是本地运行和打包方案：

### 🌟 本地打包/运行的准备工作
让电脑拥有一键运行和打包的能力（如果选择上面的 🏆 终极方案，此步不需要）：
1. **安装 Node.js 环境**：👉 [Node.js 官方免配置一键安装包](https://nodejs.org/dist/v20.11.1/node-v20.11.1-x64.msi)
2. **安装它**：双击下载的安装包，一路多次点击 **“Next（下一步）”** 并完成安装即可（只需十几秒）。

---

## 🚀 方案 A：使用 Nativefier 一键打包桌面端（最省心、无需本地服务器）

这个方案就是您关心的 **Nativefier** 打包方案！它将直接把我们目前精美完备的页面变成一个没有任何网址栏、一键拉起的**独立桌面软件窗口**：

### 1️⃣ 第一步：下载项目
* 网页右上角点击 **“齿轮”图标 (Settings)** -> 选择 **"Export to ZIP"（导出为 ZIP）**，将包下载到电脑并**右键解压**出来。

### 2️⃣ 第二步：鼠标双击一键打包
* 找到并双击 👉 `使用Nativefier一键打包.bat`
* **会有什么发生？** 黑底窗口会飞速走进度条，自动去云端抓取最轻量的网页外壳，合成精美的桌面版客户端。
* **打包完毕后：** 当前文件夹下会自动多出一个叫 **`LocalMediaManager-win32-x64`** 的独立文件夹！

### 3️⃣ 第三步：双击使用它！
* 进入该文件夹，双击里面的 👉 **`LocalMediaManager.exe`**
* **奇迹时刻**：一个完全没有浏览器地址栏、带有自定义精美边框、像 Eagle 软件一模一样的独立桌面应用窗口将直接开启！
* 它可以被拉大、还原、像正常软件一样置于任务栏。

---

## 🚀 方案 B：运行 Electron 本地离线桌面端（100% 本地盘文件存储，不需要联网）

如果您希望您的数据（导入的图片、视频）不走网络，完全存在您电脑的 C盘 或 D盘 等本地硬盘上，可以使用我们的 **Electron 本地版**：

### 1️⃣ 第一步：下载项目并解压
* 同样将导出的 ZIP 压缩包右键解压到您的电脑某个位置。

### 2️⃣ 第二步：双击运行客户端
* 双击文件夹中的 👉 `运行桌面客户端.bat`
* **会有什么发生？** 
  * 窗口会自动设置高速淘宝下载通道，把运行桌面版所需要的数据库库文件自动下载并配置好。
  * 随后，自动弹出没有任何浏览器边框、最安全的 **独立本地桌面窗口**！
* **注意**：运行期间请将这个黑色窗口最小化，不要关闭。用完后关闭桌面窗口并按任意键关闭黑窗即可！

---

## 🚀 方案 C：使用 Electron 源码静态编译成单体「本地免安装.exe」

如果您想自制一个免安装的单体 `.exe`，发送给朋友，请在解压后的文件夹内：
* 双击 👉 `构建Windows免安装桌面版.bat`
* 按照控制台走完 4 步后，会在您的文件夹下直接生产出一个叫 **`LocalMediaManager_Standalone.exe`** 的独立文件。
* 您可以删掉其他任何文件，只保留并把该 `exe` 发送给任意电脑，双击立即离线开启桌面端！

